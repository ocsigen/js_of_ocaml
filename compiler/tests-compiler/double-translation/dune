; Like the parent directory, all modules share one library and each source file
; is its own inline-test partition. New *.ml files are picked up automatically.

(library
 (name jsoo_compiler_test_double_translation)
 (libraries js_of_ocaml_compiler unix str jsoo_compiler_expect_tests_helper)
 (inline_tests
  ;; See the parent dune: depend on the profile-selected [node] wrapper.
  (deps
   %{bin:node}
   (file %{project_root}/compiler/bin-js_of_ocaml/js_of_ocaml.exe)
   (file %{project_root}/compiler/bin-jsoo_minify/jsoo_minify.exe)))
 (flags
  (:standard -open Jsoo_compiler_expect_tests_helper))
 (preprocess
  (pps ppx_optcomp_light ppx_expect_light)))
