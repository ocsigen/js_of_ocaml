(test
 (modes js)
 (build_if
  (and
   (<> %{profile} wasm)
   (<> %{profile} wasm-effects)))
 (js_of_ocaml
  (javascript_files setup.js)
  (flags :standard --setenv JSOO_C=from-jsoo-args))
 (action
  (setenv
   JSOO_B
   from-env
   (run node %{test})))
 (name test))
